feat: 改成不停止——去掉 5h58m 自杀逻辑，Actions 用自动续跑绕过 6h 上限

- run-all.sh / monitor-recovery.sh: MAX_DURATION_SEC 默认 0（= 不设上限，日志显示
  「剩余: 无限」），0/none/unlimited/inf 都按无限处理；想限制就填秒数
- 新增 scripts/continue-workflow.sh: 用 GITHUB_TOKEN 调 workflow_dispatch 重新触发
  同一个工作流（3 个 workflow 都加了「续跑下一段」步骤 + permissions: actions: write）
- keepalive.yml: max_duration_sec 默认 21000（≈5h50m，留出收尾时间）、auto_continue
  默认 true → 每段到点发完报告就自动续跑，对外表现为 24 小时不停
- keepalive-once.yml / monitor-recovery.yml: auto_continue 默认 false（单次测活 /
  恢复监控仍按原语义结束），需要长跑时手动打开
- 文档: README 增加「不会停止（自动续跑）」章节并提醒私有仓库 Actions 分钟数
  （Free 套餐 2000 分钟/月，24/7 ≈ 43200 分钟/月）；.env.example 补 MAX_DURATION_SEC
  与 CLI_RETRY_ABORT_PATTERN
- 测试: 41/41 通过（新增无限模式 2 例、续跑脚本 2 例、有上限时仍会停 1 例）
